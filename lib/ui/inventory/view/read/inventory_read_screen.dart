import 'package:flutter/material.dart';

class InventoryReadScreen extends StatelessWidget {
  const InventoryReadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
